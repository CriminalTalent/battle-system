# Server
NODE_ENV=production
HOST=0.0.0.0
PORT=3001

# CORS (쉼표로 여러 개 허용)
# 운영: 도메인만, 로컬 개발시 localhost 추가
CORS_ORIGIN=https://pyxisbattlesystem.monster

# Link generation base (선택)
# 설정하면 이 값을 사용, 비워두면 X-Forwarded-* 헤더로 자동 추정
PUBLIC_BASE_URL=https://pyxisbattlesystem.monster

# Express가 Nginx 프록시 신뢰(https 판단용)
TRUST_PROXY=1
